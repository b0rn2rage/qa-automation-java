package com.tcs.edu.decorator;

/**
 * Enum, определяющий характер дублирования:
 * DOUBLES - позволяет выводить на печать дублированные сообщения
 * DISTINCT - выводит только уникальные сообщения
 */
public enum Doubling {
    DOUBLES, DISTINCT
}
